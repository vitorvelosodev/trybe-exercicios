if (typeof array[index] !== 'number')
'number' !== 'number'
false

array[index] = 'abc';

typeof array[index] !== 'number'
typeof 'abc' !== 'number'
'string' !== 'number'
true

typeof variavel === 'function'
true
false

array = [200, 300, 400];

let soma; (soma => 'undefined')

soma += array[index];
soma = soma + array[index];
soma = 'undefined' + 200;
soma = 'undefined';

let soma = 0; (soma => 'number')

soma += array[index];
soma = soma + array[index];
soma = 0 + 200;
soma = 200;



let soma = 0;

array = [200, 300, 400];
                       3   <   3
for (let index = 0; index < array.length; index += 1) {
  console.log(array[index]);
}
executo o que tem depois
return soma;

1: index = 0; soma => 200;
2: index = 1; soma => 500;
3: index = 2; soma => 900;
4: index = 3; NAO EXECUTA


argumentos = 50;


let x = funcao(argumentos) => numero, uma String, um objeto,

funcao(50) => resultado

Math.round(valor) => resultado (arrendondamento)

return Math.round(soma / array.length);
return Math.round(5 / 3)
return Math.round(1.33333333);
return 1;





(array) => {

}

function(array) {

}

const soma = () => {
  return 1 + 1;
}

function soma() {
  return 1 + 1;
}

const soma = 2

console.log(soma)
2




const average = (array) => {
  codigo
  codigo
  codigo
  return media arrendonda
};